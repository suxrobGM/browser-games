import { Category } from "@core/enums";
import { GameConfig } from "@core/types";

export const Config: GameConfig = {
    gameId: "bugsequence",
    displayName: "Bug Sequence",
    category: Category.Memory,
    timerLimit: 45
}